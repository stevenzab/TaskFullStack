import React from "react"
import BridgeDetail from "@/components/bridge-details"

/**
 * UpdateBridge component is a component that renders the BridgeDetail component.
 * 
 * This component is used to display and update the details of a bridge in the application.
 * 
 * @returns {JSX.Element} The rendered `BridgeDetail` component wrapped in a React Fragment.
 */

export default function UpdateBridge() {
    return(
        <>
        <BridgeDetail/>
        </>
    )
}